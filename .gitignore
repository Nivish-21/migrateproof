node_modules/
dist/
.env
.DS_Store
.firecrawl/

# internal planning/session notes — not part of the public repo
docs/
MigrateProof.md

# Scratch clones created by scan's live-run verification (Task 45).
# These carry their own node_modules and .git and can reach hundreds of MB.
.temp-*/
